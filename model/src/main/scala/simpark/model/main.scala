package simpark.model



object Main extends App{
/*
  val alpha = 0.1
  val ip = 0.01
  val it = 0.01
  val l = 0.00052
  val g = 0.2
  val M = 36036.0
  val p = 0.0004
  val a = 10000.0
  val eta = 0.0008
  val phi = 1833.0
  val phi2 = 10000.0
  val del = 0.01
  val mp = 14.0
  val mt = 25.0
  val e = 100



  val eps = 100
  val zeta = 0.1
  val state = Vector(10000.0, 3000.0, 2000.0)
  */
 // val cond1 = Vector(25922.8097, 10.6397, 35721.3656)

 //   val result = DistToAttractor(Vector(zeta, l,g,M,alpha, p, a, e, eta, eps, phi,
  //    phi2, del, mp, mt, ip, it),state, 1000000)
/*
  //  val indice = result._1
   // println(indice)
    val cat = result._2
    println(cat)
  */
/*
  val parc = Parc3D(
    alpha = alpha,
    ip = ip,
    it = it,
    l = l,
    g = g,
    M = M,
    p = p,
    a = a,
    eta = eta,
    phi = phi,
    phi2 = phi2,
    del = del,
    mp = mp,
    mt = mt,
    e = e)
*/


/*


  val Vector(capital, animals, tourists) = parc.dynamic(state, Vector(zeta, eps))

  def dist(x: Double, min: Double, max: Double) =
    if(x < min) math.abs(x - min) else if(capital > max) math.abs(x - max) else 0.0


*/

/*  val fCapital = dist(capital, 5000, 100000)
  val fAnimals = dist(animals, 500, 3000)
  val fToursits = dist(tourists, 5000, 20000) */

 // val fitness = math.sqrt(fCapital * fCapital + fAnimals * fAnimals + fToursits * fToursits)

  //println(s" capital=$capital, animals=$animals, tourists=$tourists")





}
